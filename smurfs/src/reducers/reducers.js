const initialState = {
    smurfs: [],
    test: 'this is a test'
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}